'use strict';

var chai = require('chai');

require("source-map-support").install();
describe('Dummy Test', () => {
    it('should fail, because it needs to be replaced by something meaningful', () => {
        chai.expect(false).to.equal(true);
    });
});
//# sourceMappingURL=bundle.js.map